{% extends 'base.html.twig' %}
{% block title %}{{ slug }}{% endblock %}

{% block body %}

    <div class="media">
        <img class="align-self-start mr-8" src="{{program.poster}}" alt="{{ program.title }} poster">
        <div class="media-body">
            <h1 class="mt-0">{{ program.title }}</h1>
            <p>{{ program.summary }}</p>
            <p>Catégorie : {{ program.category.name }}</p>


                {% for season in program.seasons %}
                    <a href="{{ path('wild_season', {'id': season.id}) }}">{{ season.description }}</a>
                {% endfor %}



        </div>

    </div>



{% endblock %}





