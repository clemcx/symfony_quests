{% extends 'base.html.twig' %}

{% block title %}Hello CategoryController!{% endblock %}

{% block body %}


<div class="container">
    <h1>Categories</h1>

           {% for category in categories  %}
         <a href="{{ path('wild_category', {'categoryName': category.name}) }}"> <p>{{ category.id }} : {{ category.name }}</p></a>
        {% endfor %}
    <a href=" {{ path('category_add')}}"> ajouter une catégorie</a>

</div>
{% endblock %}
